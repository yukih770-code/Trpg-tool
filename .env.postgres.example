# Server-only local Postgres development example.
# Do not expose this value to Vite, Netlify frontend env, or browser bundles.

DATABASE_URL=postgres://trpg_local:trpg_local_password@localhost:55432/trpg_platform_dev
DATABASE_SSL_MODE=disable
DATABASE_POOL_MAX=5
DATABASE_CONNECTION_TIMEOUT_MS=3000
