# 当前平台阶段

更新时间：2026-08-14

## 一句话结论

项目已经进入“可部署、可恢复、单节点多人私有 Alpha”阶段：服务器、战役、房间、大厅和 Runtime 主链可运行；本地 AI Kernel 与 DND 车卡助手 v1 已形成可调用闭环，但产品尚未达到正式账号安全、公开生态、云端 AI 商业化和多实例扩展阶段。

## 当前可验收主链

```text
私有 Alpha 登录
→ 创建 / 进入服务器
→ 创建战役与房间
→ 主持人直接放置自由 Token，或玩家从角色库提交角色
→ 主持人审批，玩家 Ready
→ 进入 Runtime
→ 地图 / Token / 投骰 / 战斗 / 日志
→ 单节点数据库恢复
```

这条主链存在真实前后端、权限投影和 PostgreSQL 持久化；具体稳定性以对应 smoke 和真实双账号验收为准。

## 能力成熟度

| 领域 | 当前阶段 | 真实边界 |
| --- | --- | --- |
| DND 角色 | 创建与升级链 Alpha 可用 | 一级角色可确定性校验和原子保存；升级/多职业支持预览、校验、提交与安全撤销；完整法术选择、真实起始装备、联合施法与复杂专长效果仍待闭环 |
| COC / CP RED | 轻量可用 | 角色与部分规则 Runtime 存在，完整系统纵向流程未闭环 |
| 多人 Runtime | 单节点 Alpha | 房间、大厅、角色准入、地图、Token、战斗可用；多实例 authority 未实现 |
| 云端部署 | 私有 Alpha 准备 | 登录门、PostgreSQL、Render 路线存在；仍需持续真实多人 smoke |
| 自定义资料 | 私人创作早期 | DND 私人资料可编辑/导入/引用；通用结构化编辑与包生命周期未完成 |
| Workshop / 同人区 | 真实空库 | 有目录、类型和仓储边界；无公开发布、审核、订阅、安装、计费，不展示假作品 |
| AI | 本地 Kernel + 车卡助手 v1 | 有可调用的后端模型网关、Ollama 本地 provider、真实状态检查和 DND Builder 待确认建议链；模型不随项目捆绑，战役/Runtime AI 循环、云端 provider 与计费未实现 |

## 当前架构边界

- `CatalogObject`、`OwnedObject`、`CampaignObject`、`RuntimeObject` 不相互冒充。
- 玩家可自由创作；是否进入实际房间由主持人和房间审核决定。
- AI 只能提出建议、草稿和待确认意图，不能绕过规则引擎、权限、校验或 repository/service 写入链。
- 本地模型与云端 API 是同一内生智能层的不同计算提供方，不是两套产品。
- UI 壳、预留接口、mock/seed 和文档合同都不能被记作已实现服务。

## 最近完成

“DND 升级与多职业闭环”以及“本地 AI Kernel + DND 车卡助手 v1”已完成。Builder 现在可以向部署者配置的本地 Ollama 模型请求受限的结构化建议，经本地确定性校验与预览后，由用户明确确认才原子写入 Owned Actor，并保留有界审计和安全撤销。没有配置或安装模型时会如实显示不可用。

## 下一条完整任务

优先完成“战役 / Session AI 循环”：把备团上下文、会中日志摘要、规则定位、主持建议与会后纪要接入同一网关，并严格按观众投影读取。具体顺序见 `ECOSYSTEM_AND_AI_ROADMAP.md`。

## 事实维护规则

1. 当前代码、迁移和可重复测试是最高事实源。
2. 本文件记录阶段结论，不堆积逐次提交日志。
3. `PROJECT_STATUS.md` 记录细节；旧盘点与归档只作历史参考。
4. 新任务若改变阶段判断，必须同步更新本文件、测试清单和项目状态后再提交。
