# 当前平台阶段

更新时间：2026-08-19

## 一句话结论

项目已经进入“可部署、可恢复、单节点多人私有 Alpha”阶段：服务器、战役、房间、大厅和 Runtime 主链可运行；本地 AI Kernel 已接入 DND 车卡与主持人 Session 辅助的首批真实闭环，但产品尚未达到正式账号安全、公开生态、云端 AI 商业化和多实例扩展阶段。

## 当前可验收主链

```text
私有 Alpha 登录
→ 创建 / 进入服务器
→ 创建战役与房间
→ 主持人直接放置自由 Token，或玩家从角色库提交角色
→ 主持人审批，玩家 Ready
→ 进入 Runtime
→ 地图 / Token / 投骰 / 战斗 / 日志
→ 单节点数据库恢复
```

这条主链存在真实前后端、权限投影和 PostgreSQL 持久化；具体稳定性以对应 smoke 和真实双账号验收为准。

## 能力成熟度

| 领域 | 当前阶段 | 真实边界 |
| --- | --- | --- |
| DND 角色 | 创建与升级链 Alpha 可用 | 一级角色可确定性校验和原子保存；升级/多职业支持预览、校验、提交与安全撤销；完整法术选择、真实起始装备、联合施法与复杂专长效果仍待闭环 |
| COC / CP RED | 轻量可用 | 角色与部分规则 Runtime 存在，完整系统纵向流程未闭环 |
| 多人 Runtime | 单节点 Alpha | 房间、大厅、角色准入、地图、Token、战斗可用；d20 检定支持服务端权威的优势 / 劣势 / DC；实时 Session 的写入权威唯一，Campaign 侧 runtime API 在房间实时期间只读；攻击对 AC、伤害结算与角色数值推导仍未接入；多实例 authority 未实现 |
| 云端部署 | 私有 Alpha 准备 | 登录门、PostgreSQL、Render 路线存在；仍需持续真实多人 smoke |
| 自定义资料 | 私人创作早期 | DND 私人资料可编辑/导入/引用；通用结构化编辑与包生命周期未完成 |
| Workshop / 同人区 | 真实空库 | 有目录、类型和仓储边界；无公开发布、审核、订阅、安装、计费，不展示假作品 |
| AI | 本地 Kernel + 模型路由设置 + 车卡 / Room Session 助手 v1 | 认证目录投影已支持设备级关闭、自动和指定本地模型；自动优先已安装 Qwen 3.6。DND Builder 建议可确定性确认；活跃主持人可从服务端投影日志生成并确认 Session 草稿。规则定位、战役资料检索、云端 provider 与计费仍未实现 |

## 当前架构边界

- `CatalogObject`、`OwnedObject`、`CampaignObject`、`RuntimeObject` 不相互冒充。
- 玩家可自由创作；是否进入实际房间由主持人和房间审核决定。
- AI 只能提出建议、草稿和待确认意图，不能绕过规则引擎、权限、校验或 repository/service 写入链。
- 本地模型与云端 API 是同一内生智能层的不同计算提供方，不是两套产品。
- UI 壳、预留接口、mock/seed 和文档合同都不能被记作已实现服务。

## 最近完成

“Runtime 事件单一写入路径 v1（T7）”已完成：实时房间运行期间，Campaign 侧 runtime API 不再是第二条权威写入路径。对该实时 Session 的追加、创建与更新一律返回 409（`error.reason` 为 `session_owned_by_live_room`），读取保持开放；`room.runtimeLog.` 与 `room.mapEvent.` 这两个仅由实时房间持久化模块产出、并被重启恢复信任的保留命名空间，对任何 Session 一律返回 400（`error.reason` 为 `reserved_runtime_event_kind`），且该判断先于 Session 查找、与房间是否实时无关。被拒绝的写入不产生广播、不移动 `latestSeq`。该能力在处理器构造处是必填项，显式退出只有 `NO_LIVE_ROOM_REGISTRY` 一种写法，遗漏即编译失败。没有新增 schema、迁移、事件种类或依赖，前端未改动。

“Room Runtime 服务端权威 d20 检定 v1”已完成：真实房间里的 d20 检定可以选择普通 / 优势 / 劣势并给出可选 DC，骰面、保留骰、总值与成败全部由服务器 crypto RNG 判定，客户端只提交意图。事件仍是既有的 `dice.roll`，新增字段全部可选，旧事件与旧读取方不受影响；天然 20 / 天然 1 只作为元数据记录，不产生自动成功或自动失败。发起掷骰的玩家现在直接看到服务器判定结果，不必再翻日志抽屉。

“DND 升级与多职业闭环”“本地 AI Kernel + DND 车卡助手 v1”“Room Session AI 助手 v1”以及“AI 设置与模型路由 v1”已完成。设置页从认证后端读取安全模型目录，设备偏好不能扩大资料或房间权限；Session 助手只向活跃主持人开放，服务器从主持人本来可见的 RuntimeLog 投影构造有界上下文。没有配置、无法连接或没有安装允许模型时都会如实显示不同不可用状态。

## 下一条完整任务

继续完成“战役 / Session AI 循环”的第二条链：建立有来源、有权限的战役资料检索与规则定位，并把可长期保留的备团/会后成果接入 GeneratedArtifact 生命周期。不能把模型记忆或未授权规则内容冒充检索结果。具体顺序见 `ECOSYSTEM_AND_AI_ROADMAP.md`。

## 事实维护规则

1. 当前代码、迁移和可重复测试是最高事实源。
2. 本文件记录阶段结论，不堆积逐次提交日志。
3. `PROJECT_STATUS.md` 记录细节；旧盘点与归档只作历史参考。
4. 新任务若改变阶段判断，必须同步更新本文件、测试清单和项目状态后再提交。
