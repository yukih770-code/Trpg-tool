# 生态与内生 AI 路线

更新时间：2026-08-14

## 产品目标

打造一个从角色与资料创作，到战役组织、多人跑团、内容复用与创作者生态连成一体的多系统 TRPG 平台。

生态不是先做一个空市场。顺序必须是：个人真实创作可用 → 私人包可流转 → 房间安全准入 → 公开发布审核 → 订阅/更新 → 创作者商业化。

## AI 的产品身份

AI 是平台内部的智能执行层和工作催化剂，不是单独放在菜单里的聊天机器人，也不是把本地模型包装成用户服务。

它逐步进入：

- 车卡：解释选择、发现冲突、补全草稿、生成待确认方案。
- 备团：整理资料、生成 NPC/地点/场景草稿、建立关系和可检索上下文。
- 跑团：基于当前投影与日志提供主持建议、规则定位、摘要和待确认操作。
- 创作：把自然语言或导入资料变成结构化草稿，并经过验证、预览和人工确认。
- 其他项目：复用同一上下文、工具、审计与模型路由基础，而不是复制 AI 集成。

### 不可破坏的边界

```text
用户意图
→ AI 生成建议 / 草稿 / Proposed Intent
→ 确定性 schema 与规则校验
→ 权限与作用域检查
→ 用户 / 主持人确认
→ service / repository 写入
→ 审计与可恢复结果
```

AI 不直接改角色、房间、战斗、权限或账单。提示词不是权限系统；模型输出不是已提交对象；生成文本不是规则事实。

## 本地模型与云端 API

两者通过同一内部 Model Gateway：

| 路由 | 定位 | 适用 |
| --- | --- | --- |
| 本地模型 | 默认私密、低边际成本、可离线的内部计算 | 摘要、分类、草稿、低风险辅助、敏感本地上下文 |
| 云端 API | 用户明确启用的更强计算与商业增强 | 高质量长上下文、复杂生成、多模态、需要稳定吞吐的任务 |

Gateway 负责能力声明、模型选择、取消/超时、预算、隐私策略、可观测性和回退。上层车卡、Runtime、创作工具不直接依赖某个供应商 SDK。

## 完整链条任务路线

每一项都必须从入口做到验证、写入/审计、UI 状态、测试和文档后再独立提交。能服务同一用户结果的工作合并在一项，不拆成只写接口或只画页面的小任务。

### 第一阶段：把核心游戏体验做成可靠产品

1. **产品事实与路线统一**：当前阶段文档、内生 AI/生态路线、Workshop/同人区真实空态、移除虚构社区内容。
2. **DND 一级角色创建闭环 v1（已完成）**：当前 schema 内的物种、背景、属性、职业、专长确定性校验与原子保存已闭环；法术/装备缺口显式警告，完成后可打开角色卡并进入房间角色选择。完整法术表与真实起始装备生成仍属于后续数据/物品链。
3. **DND 升级与多职业闭环（已完成）**：等级计划、子职业、专长/属性提升、法术与职业资源的逐级变更、审计和安全回退已闭环；多职业联合施法与复杂自动效果继续显式留待人工复核。
4. **本地 AI Kernel + DND 车卡助手（已完成 v1）**：统一模型网关、Ollama 本地 provider、受限上下文包、结构化建议、确定性预览、确认后原子写入与本地审计已闭环；认证模型目录和设备级关闭/自动/指定本地模型设置也已闭环，自动路由优先已安装 Qwen 3.6。模型仍需部署者另行安装；云端 provider 与计费未实现。
5. **战役 / Session AI 循环（首条 Runtime 链已完成）**：活跃主持人已可基于服务端投影 RuntimeLog 生成备团提纲、会中建议和会后纪要，预览后明确选择私密/公开，并经游标防陈旧校验追加日志。尚待完成有权限的战役资料检索、带来源的规则定位，以及可长期保留/版本化的 GeneratedArtifact 成果库；因此本项仍在进行中。
6. **AI 结构化创作闭环（DND 私人内容首链已完成）**：现有十类 DND 私人条目已接通有界生成、确定性校验、预览和显式填入未保存表单，并继续复用既有版本保存与房间审核；通用跨系统 schema、依赖语义和公开发布仍待后续链条。

### 第二阶段：私人生态变成可流转系统

7. **通用私人包生命周期**：创建、导入预览、版本、依赖、启用/禁用、房间引用、导出与回滚。
8. **正式身份与安全**：正式账户、凭据、会话、邀请、风控与恢复，不再依赖私有 Alpha 访问码模型。
9. **媒体与对象存储**：头像、Token、地图、音频、文档附件的变体、可见性、备份和删除生命周期。
10. **公开创作者生态**：作者主页、发布审核、许可证/来源、真实目录、收藏/订阅、依赖与更新；没有真实内容时保持空态。
11. **云端 AI 付费闭环**：明确授权、套餐/额度、用量账本、预算上限、失败补偿、供应商成本与用户可见账单。
12. **创作者商业化**：支付、分成、退款、税务/合规接口、版权投诉和下架恢复链路。

### 第三阶段：扩展运行规模与系统覆盖

13. **多实例 Runtime Authority**：跨进程房间 authority、事件分发、幂等写入、故障接管和可观测性。
14. **COC 完整纵向闭环**：从调查员创建、技能/理智/成长到场景、线索、追逐和结团记录。
15. **CP RED 完整纵向闭环**：角色、装备/义体、战斗、Netrunning、任务与经济流程。
16. **冒险者公会层**：跨服务器发现、长期身份、活动与创作者/玩家关系，但不破坏服务器和房间 authority。

## 每项任务的完成定义

一个逻辑任务只有同时具备以下内容才算完成：

1. 用户入口与清晰的页面责任。
2. 权限、作用域和上下文来源。
3. service/repository 或明确的确定性核心。
4. schema/规则校验与失败分类。
5. 必要的持久化、幂等、审计或可恢复策略。
6. UI 的 loading、empty、error、cancel、confirm 状态。
7. 自动 smoke/单测与高风险真实流程。
8. 当前阶段、项目状态和测试清单同步。
9. 独立提交，且不混入 `output/`、`tools/`、`work/`。

仅有类型、接口、页面壳、假数据或文档不算能力完成。

## 近期追赶指标

- 用户能在不理解内部架构的情况下完成一次 DND 角色创建并加入房间。
- 主持人能同时使用自由 Token 与批准角色 Token，不被强迫先造角色。
- 本地 AI 在车卡任务中能给出可解释、可取消、可审计且必须确认的结构化建议。
- 私人资料能跨角色、战役与房间复用，并保留版本与准入判断。
- Workshop 第一个公开作品必须来自真实发布链，而不是 seed 或演示卡片。
